[package]
name = "w_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
glfw = "0.61"
gl = "0.14.0"
glam = "0.30.9"
image = "0.25.9"

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

uuid = { version = "1.10", features = ["v4", "fast-rng"] } 

mlua = { version = "0.11.5", features = ["vendored", "luajit"] }